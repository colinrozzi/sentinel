[package]
name = "crashing-child"
version = "0.1.0"
edition = "2021"

[lib]
name = "crashing_child"
crate-type = ["cdylib"]

[dependencies]
packr-guest = { version = "0.8.1", features = ["derive", "pic"] }
